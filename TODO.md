# Project TODOs

## Email

- [ ] **Integrate Gmail API** — Create drafts programmatically with full HTML body prefilled (no clipboard paste). Would replace the current compose-URL + clipboard workaround, which is limited by URL length and plain-text `body` params. Requires OAuth setup (Google Cloud project, credentials, token storage).
